import 'package:flutter/material.dart';

class DogLoversApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: DogLoverMainPage(),
    );
  }
}
