import 'package:flutter/material.dart';

import 'payment_main_page.dart';

class PaymentApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: PaymentMainPage(),
    );
  }
}
